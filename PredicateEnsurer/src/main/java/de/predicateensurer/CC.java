package de.predicateensurer;

public class CC {
	/**
	 * This method belongs to the CogniCrypt plugin and inserts a predicate for a certain object. 
	 * @param var - name of the variable
	 * @param predicate - predicate for the variable
	 * @return the return value is not relevant
	 */
	public static boolean ensuresPredicate(String var, String predicate) {
		return true;
	}
}
